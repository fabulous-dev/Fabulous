<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- Base theme applied no matter what API -->
  <style name="FabulousContactsTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
    <item name="windowNoTitle">true</item>
    <!--We will be using the toolbar so no need to show ActionBar-->
    <item name="windowActionBar">false</item>
    <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette -->
    <!-- colorPrimary is used for the default action bar background -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <!-- colorAccent is used as the default value for colorControlActivated
     which is used to tint widgets -->
    <item name="colorAccent">@color/colorAccent</item>
    <!-- You can also set colorControlNormal, colorControlActivated
     colorControlHighlight and colorSwitchThumbNormal. -->
    <item name="windowActionModeOverlay">true</item>
    <item name="android:datePickerDialogTheme">@style/FabulousContactsTheme.AppCompatDialogStyle</item>
    <item name="android:autoCompleteTextViewStyle">@style/FabulousContactsTheme.AutoCompleteTextViewStyle</item>
  </style>
  <style name="FabulousContactsTheme.AppCompatDialogStyle" parent="Theme.AppCompat.Light.Dialog">
    <item name="colorAccent">@color/colorAccent</item>
  </style>
  <style name="FabulousContactsTheme.AutoCompleteTextViewStyle" parent="Widget.AppCompat.Light.AutoCompleteTextView">
    <!-- Gets rid of the "underline" in the text -->
    <item name="android:textColor">@android:color/white</item>
  </style>
  <style name="FabulousContactsTheme.Splash" parent="FabulousContactsTheme">
    <item name="android:windowBackground">@drawable/splash_background</item>
  </style>
</resources>